# The root-owned parent gates connect access even if nginx creates a 0666 socket.
# Only the connector gets membership in ghostcloak-tunnel; no login users.
d /run/ghostcloak-tunnel 0750 root ghostcloak-tunnel -
