# Authentication boundary

Not implemented. Future account credentials authorize mailbox operations, never decrypt messages. Compromised authentication is not authorization to replace a pinned contact key. Phone-number signup and recovery are out of scope.
